# docs

Supporting documentation for Bikespot.

- [`adr/`](adr/) — Architecture Decision Records.
- `screenshots/` — images used in the root README (see below).

## Screenshots — TODO (needs real captures)

The root README references screenshots that are **not yet committed**. Capture
them from a real device so they are honest, then drop them in `screenshots/`
with these exact filenames:

| File | What to capture |
| --- | --- |
| `screenshots/home-light.png` | Home screen with a saved spot, light theme |
| `screenshots/home-dark.png` | Same view, dark theme |

Suggested capture:

1. Open <https://bikespot.duinlab.nl> on a phone (or a mobile-sized browser window).
2. Save one realistic spot so the screen isn't empty.
3. Screenshot in each theme (toggle via the app's theme control).
4. Trim to the viewport; keep both images the same width.

Until these exist, the image links in the root README will render as broken —
replace this section's status once they are added.
